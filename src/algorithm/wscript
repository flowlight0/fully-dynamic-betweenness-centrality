# -*- python -*-

srcdir= '.'
blddir= '../../bin'

def build(bld):
    bld.stlib(
        source = [
            'centrality.cpp',
            'centrality_sample.cpp',
            'dynamic_centrality_bms.cpp'
        ],
        includes = ['../'],
        target  = 'algorithm')


    

    
    

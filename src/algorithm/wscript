# -*- python -*-

srcdir= '.'
blddir= '../../bin'

def build(bld):
    bld.stlib(
        source = [
            'betweenness_centrality.cpp'
        ],
        includes = ['../'],
        target  = 'algorithm')


    

    
    
